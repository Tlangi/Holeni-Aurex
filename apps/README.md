# Applications

The Angular CLI will generate the browser application into `apps/web`. The target
directory is intentionally empty so the CLI can scaffold it without file conflicts.
See `docs/IMPLEMENTATION-PLAN.md` for the exact command.
