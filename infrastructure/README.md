# Infrastructure

Local and server deployment configuration will live here. Configuration must use
dedicated ports, service identities, paths, and logs so it does not affect other
applications on the host.
